namespace Convert;

class Program
{
    static void Main(string[] args)
    {
        // double g = 9.8;
        // int h = ToInt32(g);
        // WriteLine($"g is {g}, h is {h}");
        
        
            // Allocate an array of 128 bytes.
//         byte[] binaryObject = new byte[128];
// // Populate the array with random bytes.
//         Random.Shared.NextBytes(binaryObject); 
//         WriteLine("Binary Object as bytes:");
//         for (int index = 0; index < binaryObject.Length; index++)
//         {
//             Write($"{binaryObject[index]:X2} ");
//         }
//         WriteLine();
// // Convert the array to Base64 string and output as text.
//         string encoded = ToBase64String(binaryObject);
//         WriteLine($"Binary Object as Base64: {encoded}");

        //try parse 
        
        // Write("How many eggs are there? ");
        // string? input = ReadLine();
        // if (int.TryParse(input, out int count))
        // {
        //     WriteLine($"There are {count} eggs.");
        // }
        // else
        // {
        //     WriteLine("I coult not parse the input.");
        // }

        // int number = int.Parse("123");
        // bool succes = int.TryParse("123", out int numberWithTry);
        // bool successUri = Uri.TryCreate("https://local-host:5000/api/customers", UriKind.Absolute,out Uri? serviceUrl);
        
        
        
        

    }
    
}